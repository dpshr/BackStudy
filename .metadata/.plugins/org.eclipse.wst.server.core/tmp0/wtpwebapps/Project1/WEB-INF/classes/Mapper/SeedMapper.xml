<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
   <mapper namespace="mapper.SeedMapper">
	<select id="Seed_price" parameterType="SeedDTO" resultType="Integer">
  		select seed_price from tbl_farm_seed where seed_name = #{seed}
  	</select>
   </mapper>