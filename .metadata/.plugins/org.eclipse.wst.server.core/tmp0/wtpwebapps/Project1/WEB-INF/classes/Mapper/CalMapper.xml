<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
   <mapper namespace="mapper.CalMapper">
	  <select id="Mc_cost" parameterType="MachineDTO" resultType="Integer">
  		select machine_price from tbl_farm_machine where machine_use = #{machine_use}
  	</select>
   </mapper>